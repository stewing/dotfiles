# stewing@outlook.com zshrc

# General Configuration
bindkey -e              # set emacs/readline-style editing
set -k                  # allow comments in interactive mode

# Fuzzy Finder
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh
